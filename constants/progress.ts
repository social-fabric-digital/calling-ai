// Shared progress model for the pre-onboarding intro + onboarding flow.
// Keep these in one place so header bars remain synced across screens.
export const FEATURES_INTRO_TOTAL_CARDS = 4;
export const ONBOARDING_TOTAL_STEPS = 28;

export const FULL_ONBOARDING_JOURNEY_UNITS =
  FEATURES_INTRO_TOTAL_CARDS + ONBOARDING_TOTAL_STEPS;
